name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Least privilege: CI only reads the repo. Nothing here writes to it.
permissions:
  contents: read

# One run per ref: a new push cancels the in-flight run for the same branch/PR
# instead of queueing behind it. main is exempt (never cancel a landed build).
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  backend:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    services:
      # The unit suite runs on real Postgres with the Alembic schema applied,
      # so migration-only constraints (partial unique indexes, VARCHAR
      # widths) are enforced in tests exactly as in production. The database
      # name contains "test" - the harness refuses non-test databases.
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: wardress
          POSTGRES_PASSWORD: wardress-test
          POSTGRES_DB: postgres
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U wardress"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      WARDRESS_TEST_DATABASE_URL: postgresql+asyncpg://wardress:wardress-test@localhost:5433/postgres_test
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v8
        with:
          version: "0.9.2"
          enable-cache: true
          cache-dependency-glob: backend/uv.lock
      - name: Sync dependencies (frozen)
        run: uv sync --frozen
      - name: Lint (ruff)
        run: |
          uv run ruff check .
          uv run ruff format --check .
      - name: Dependency audit (pip-audit)
        # Audits the locked environment. `|| true` is NOT used — a known
        # vulnerability fails CI and must be triaged in PROGRESS.md.
        run: uv run pip-audit --skip-editable
      - name: Tests
        run: uv run pytest -q

  # Migrations run against real Postgres (production's DB). The backend unit
  # suite shares this dialect (see the backend job's postgres service); this
  # job additionally exercises the migration chain itself: fresh upgrade,
  # downgrade round-trip of the newest revision, and model/migration drift.
  migrations:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: backend
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: wardress
          POSTGRES_PASSWORD: ci-postgres
          POSTGRES_DB: wardress
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U wardress"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql+asyncpg://wardress:ci-postgres@localhost:5432/wardress
      JWT_SECRET: ci-placeholder-not-a-real-secret
      CREDENTIALS_ENCRYPTION_KEY: ci-placeholder-encryption-key
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v8
        with:
          version: "0.9.2"
          enable-cache: true
          cache-dependency-glob: backend/uv.lock
      - name: Sync dependencies (frozen)
        run: uv sync --frozen
      - name: Apply migrations (fresh DB -> head)
        run: uv run alembic upgrade head
      - name: Round-trip the latest migration (down then up)
        # Proves the newest migration's downgrade is real and reversible —
        # including dropping Postgres enum types autogenerate forgets.
        run: |
          uv run alembic downgrade -1
          uv run alembic upgrade head
      - name: No model/migration drift
        # Fails if the ORM models have diverged from the migration chain
        # (a table/column/enum changed but no migration was written).
        run: uv run alembic check

  frontend:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v6
        with:
          version: 11.13.1
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: Dependency audit
        run: pnpm audit --audit-level high
      - name: Lint
        run: pnpm lint
      - name: Typecheck
        run: pnpm exec tsc -b
      - name: Tests
        run: pnpm exec vitest run
      - name: Build
        run: pnpm build

  docker:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - name: Validate compose file
        run: docker compose config --quiet
        env:
          POSTGRES_PASSWORD: ci-placeholder
          DATABASE_URL: postgresql+asyncpg://wardress:ci-placeholder@db:5432/wardress
          JWT_SECRET: ci-placeholder
          CREDENTIALS_ENCRYPTION_KEY: ci-placeholder
